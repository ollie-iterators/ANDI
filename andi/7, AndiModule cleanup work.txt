## Work on AndiModule.cleanup ##

AndiModule.cleanup = function(testPage, element, tableModule){
    if (element) {
        var elementAttrs = $(element).getAttributeNames();
        $(element).removeAttr(tableModule.attributesToRemove);
    }
};